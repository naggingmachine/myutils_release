# Deployment

`docs/` is published to https://www.myutils.app (see vercel.json).

- Commit messages in this repository must be **in English** and describe only what is new or improved, in one short line.
- The source repository publishes here via `scripts/publish-release.sh <version> <notes.md> "<one-line summary>"`.
